
#==============================================================================
# DBMS Name        :    AzureSQL SQL Server
# Template         :    wsl_sqlserver_pyscript_load
# Template Version :    8.5.1.0
# Description      :    Load load_product_line
# Generated by     :    WhereScape RED Version 10.6.0.2 (build 251121-205943)
# Generated for    :    Employee Sultan Shiffa
# Generated on     :    Tuesday, January 13, 2026 at 10:30:10
# Author           :    Sultan
# Source Schema    :    dbo
# Source Table     :    product_line
#==============================================================================
# Notes / History
#


import os
import sys
import pyodbc
import glob
from datetime import datetime
import shutil
import json
import traceback


path_to_WslPythonCommon = r"$WSL_SCRIPT_WslPythonCommon_CODE$"
path_to_SQLServer = r"$WSL_SCRIPT_WslPythonSQLServer_CODE$"

import WslPythonCommon
import WslPythonSQLServer

if sys.platform == 'win32':
    WslPythonCommon.HideWindow()


# ========================================================
# = Check Source Connection & Assign Run Mode
# ========================================================
runMode = "Database"




#--============================================================================
#-- Init Logger
#--============================================================================
debugModeExt = WslPythonCommon.GetExtendedProperty("AZ_DEBUG_MODE", str(os.environ.get('WSL_LOAD_TABLE',''))).upper().strip()

interactiveLog = ''
if debugModeExt == 'TRUE':
    debugMode = True
else:
    debugMode = False

def write_audit(message = '', logType = 'audit', statusCode = 'I'):
    # statusCodes 'E' = error, 'W' Warning, 'I' information, 'S' Success
    global interactiveLog
    if is_red_interactive():
        #message = ' : '.join([logType,statusCode,message])
        interactiveLog = '\n'.join([interactiveLog, message])
    else:
        outputJson = json.dumps({"type": logType, "message": message, "statusCode": statusCode})
        print(outputJson, flush=True)

def write_error(message = ''):
    write_audit(message, 'audit', 'E')

def write_detail(message = '', statusCode = 'I'):
    if debugMode:
        write_audit(message, 'detail', statusCode)

def write_result(message = '', statusCode = 'S'):
    write_audit(message, 'result', statusCode)

def is_red_interactive():
    
    if os.environ.get('WSL_JOB_KEY','') == '0' and os.environ.get('WSL_JOB_NAME','') == 'Develop':
        return True
    else:
        return False
    

def exit_script(exitCode = 0, message = 'Executed the script'):
    if is_red_interactive():
        if exitCode != 0:
            print(-2, flush=True)
        else:
            print(1, flush=True)
        print(message, flush=True)
        print(interactiveLog, flush=True)
        sys.exit(0)
    else:
        if exitCode != 0:
            write_result(message,'E')
        else:
            write_result(message,'S')
        sys.exit(exitCode)






#--============================================================================
#-- Create Load Properties Object For Source Connection
#--============================================================================
loadTableProperties = {}

# -- Header --------------------------------------------
if "false" == "true":
    loadTableProperties['fieldHeading'] = 2
elif "false" == "false":
    loadTableProperties['fieldHeading'] = 1

# -- Source Path --------------------------------------------
loadTableProperties['sourceFilePath'] = r" ".strip()



# -- Field Enclosure --------------------------------------------
loadTableProperties['sourceFileFieldEnclosure'] = '"'

# -- Record Delimiter --------------------------------------------
loadTableProperties['sourceFileRecordDelimiter'] = """"""
if loadTableProperties['sourceFileRecordDelimiter'].strip() == "":
    loadTableProperties['sourceFileRecordDelimiter'] ="\\n"

# -- Field Delimiter --------------------------------------------
loadTableProperties['sourceFileFieldDelimiter'] = ''

# -- File Name --------------------------------------------
loadTableProperties['sourceFileName'] = r'product_line'

# -- File Type(Parser) --------------------------------------------
loadTableProperties['fileType'] = r'csv'

loadTableProperties['waitTime'] = 1


# -- Load Table Properties --------------------------------------------
loadTableProperties['targetTableName'] = str(os.environ.get('WSL_LOAD_FULLNAME',''))
loadTableProperties['WSL_WORKDIR'] = os.environ.get('WSL_WORKDIR','')
loadTableProperties['auditLog'] = str(os.environ.get('WSL_LOAD_TABLE','')) + "_" + str(os.environ.get('WSL_SEQUENCE','')) + ".txt"













#--============================================================================
#-- Create Extended Properties Object
#--============================================================================

try:
    
    extendedProperty = {
        'DEBUG'                  : WslPythonCommon.GetExtendedProperty("AZ_DEBUG_MODE",str(os.environ.get('WSL_LOAD_TABLE',''))).upper().strip(),
        'UNICODE_SUPPORT'        : WslPythonCommon.GetExtendedProperty("AZ_UNICODE_SUPPORT",str(os.environ.get('WSL_LOAD_TABLE',''))),
        'UNLOAD_DELIMITER'       : WslPythonCommon.GetExtendedProperty("AZ_UNLOAD_DELIMITER",str(os.environ.get('WSL_LOAD_TABLE',''))),
        'UNLOAD_ENC'             : WslPythonCommon.GetExtendedProperty("AZ_UNLOAD_ENCLOSED_BY",str(os.environ.get('WSL_LOAD_TABLE',''))),
        'UNLOAD_ESC'             : WslPythonCommon.GetExtendedProperty("AZ_UNLOAD_ESCAPE_CHAR",str(os.environ.get('WSL_LOAD_TABLE',''))),
        'UNLOAD_RECORD_CHAR'     : WslPythonCommon.GetExtendedProperty("AZ_UNLOAD_RECORD_CHAR",str(os.environ.get('WSL_LOAD_TABLE',''))),
        'SPLIT_THRESHOLD'        : WslPythonCommon.GetExtendedProperty("AZ_SPLIT_THRESHOLD",str(os.environ.get('WSL_LOAD_TABLE',''))),
        'FILE_COUNT'             : WslPythonCommon.GetExtendedProperty("AZ_SPLIT_COUNT",str(os.environ.get('WSL_LOAD_TABLE',''))),
        'BLOB_STORAGE_ACCOUNT'   : WslPythonCommon.GetExtendedProperty("BLOB_ACCOUNT",str(os.environ.get('WSL_LOAD_TABLE',''))),
        'BLOB_STORAGE_ACCESS_KEY': WslPythonCommon.GetExtendedProperty("BLOB_KEY",str(os.environ.get('WSL_LOAD_TABLE',''))),
        'BLOB_STORAGE_CONTAINER' : WslPythonCommon.GetExtendedProperty("BLOB_TEMP_CONTAINER",str(os.environ.get('WSL_LOAD_TABLE',''))),
        'BLOB_DATA_SOURCE'       : WslPythonCommon.GetExtendedProperty("TEMP_DATA_SOURCE",str(os.environ.get('WSL_LOAD_TABLE',''))),
        'BLOB_ENDPOINT'          : WslPythonCommon.GetExtendedProperty("BLOB_ENDPOINT",str(os.environ.get('WSL_LOAD_TABLE','')))
    }

    if str(extendedProperty['UNLOAD_DELIMITER']) == "":
        extendedProperty['UNLOAD_DELIMITER'] = '|'


    if str(extendedProperty['UNLOAD_RECORD_CHAR']) == "":
        extendedProperty['UNLOAD_RECORD_CHAR'] = '\\n'


    if str(extendedProperty['UNLOAD_ENC']) == "":
        extendedProperty['UNLOAD_ENC'] = '"'


    if extendedProperty['UNLOAD_ESC']=="":
        extendedProperty["UNLOAD_ESC"] = "#"


    unicode = False
    if str(extendedProperty['UNICODE_SUPPORT']).upper() == "TRUE":
        unicode = "utf-8"
    else:
        unicode = "ascii"


    if extendedProperty['SPLIT_THRESHOLD']=="" or int(extendedProperty['SPLIT_THRESHOLD'])==0 :
        extendedProperty['SPLIT_THRESHOLD'] = 100000
    else:
        extendedProperty['SPLIT_THRESHOLD'] = int(extendedProperty['SPLIT_THRESHOLD'])

    if str(extendedProperty['BLOB_ENDPOINT']) == "":
        extendedProperty['BLOB_ENDPOINT'] = "core.windows.net"
    else:
        extendedProperty['BLOB_ENDPOINT'] = extendedProperty['BLOB_ENDPOINT']

except Exception as exceptionError:
    write_error("Failed to create extended properties object: " + str(exceptionError))
    write_error(traceback.format_exc())
    exitCode = 2
    exitMessage = 'Script Failed to create extended properties object'
    raise exceptionError


#--============================================================================
#-- Write Load Details To Log
#--============================================================================
write_audit("=================== LOAD OPTIONS ===================")
write_audit("Specified Load Table:        " + str(os.environ.get('WSL_LOAD_TABLE','')))
write_audit("Specified Work Dir:          " + str(loadTableProperties['WSL_WORKDIR']))
write_audit("Specified Sequence:          " + str(os.environ.get('WSL_SEQUENCE','')))
write_audit("Specified Metadata ODBC DSN: " + str(os.environ.get('WSL_META_DSN','')))
write_audit("Specified Metadata Username: " + str(os.environ.get('WSL_META_USER','')))
write_audit("Specified Metadata Password: " + ('*'*len(str(os.environ.get('WSL_META_PWD','')))))
write_audit("")

write_audit("=================== MODES ===================")
write_audit("Debug Mode:        " + str(extendedProperty['DEBUG']))
write_audit("Run Mode:          " + runMode)
write_audit("Unicode Extract:   " + unicode)
write_audit("")


write_audit("=================== SOURCE TABLE INFO ===================")
write_audit("Source Schema:               " + str(os.environ.get('WSL_SRC_SCHEMA','')))
write_audit("Source Tables:               " + 'product_line')
write_audit("=================== SOURCE DB INFO ===================")
write_audit("ODBC Source DSN:             " + str(os.environ.get('WSL_SRC_DSN','')))
write_audit("ODBC Source Username:        " + str(os.environ.get('WSL_SRC_USER','')))
write_audit("ODBC Source Password:        " + ('*'*len(str(os.environ.get('WSL_SRC_PWD','')))))
write_audit("")

write_audit("=================== EXTENDED PROPERTIES ===================")
write_audit("DEBUG_MODE:               " + str(extendedProperty['DEBUG']))
write_audit("UNICODE_SUPPORT:          " + str(extendedProperty['UNICODE_SUPPORT']))
write_audit("UNLOAD_DELIMITER:         " + str(extendedProperty['UNLOAD_DELIMITER']))
write_audit("UNLOAD_ENC:               " + str(extendedProperty['UNLOAD_ENC']))
write_audit("UNLOAD_ESC:               " + str(extendedProperty['UNLOAD_ESC']))
write_audit("UNLOAD_RECORD_CHAR:       " + str(extendedProperty['UNLOAD_RECORD_CHAR']))
write_audit("")
if str(os.environ.get('WSL_TGT_CONSTRING','')) != "":
    write_audit("Target connected with advanced connect method")
if str(os.environ.get('WSL_META_CONSTRING','')) != "":
    write_audit("Metadata connected with advanced connect method")
if str(os.environ.get('WSL_SRC_CONSTRING','')) != "":
    write_audit("Source connected with advanced connect method")

#--============================================================================
#-- SQL Server Connection Function
#--============================================================================
def execute_sql_query(sql, dsn=str(os.environ.get('WSL_TGT_DSN','')), uid=str(os.environ.get('WSL_TGT_USER','')), pwd=str(os.environ.get('WSL_TGT_PWD',''))):
    try:
        if os.environ.get('WSL_TGT_CONSTRING','') != "":
            connection_string = os.environ.get('WSL_TGT_CONSTRING','')
        else:
            connection_string = "DSN=" + dsn
            if uid:
                connection_string += ";UID=" + uid
            if pwd:
                connection_string += ";PWD=" + pwd
        connection = pyodbc.connect(connection_string, autocommit=True)
        cursor = connection.cursor()
        result = cursor.execute(sql)
        return result.rowcount
    except Exception as exceptionError:
        raise exceptionError



def create_temp_table():
    temp_table_sql = """
    CREATE TABLE load_product_line_TMP                  (     product_line_code varchar(20)
                  ,     product_line_description varchar(255)
                  ,     creating_employee_id int
                  ,     created_datetime datetime
                  ,     last_change_employee_id int
                  ,     last_change_datetime datetime
                  )
    """

    write_detail(temp_table_sql)
    try:
        execute_sql_query(temp_table_sql)
    except Exception as exceptionError:
        write_error("Failed to create temp table: " + str(exceptionError))
        write_error(traceback.format_exc())
        exitCode = 2
        exitMessage = 'Script failed to create temporary table'
        raise exceptionError


# Drop Temporary Table
def drop_temp_table():
    temp_table_sql = """
    DROP TABLE IF EXISTS load_product_line_TMP
    """
    write_detail(temp_table_sql)
    try:
        execute_sql_query(temp_table_sql)
    except Exception as exceptionError:
        write_error("Failed to drop temp table: " + str(exceptionError))
        write_error(traceback.format_exc())
        exitCode = 2
        exitMessage = 'Script failed to drop temporary table'
        raise exceptionError


def load_data(filePath):

    fieldDelimiter = str(extendedProperty['UNLOAD_DELIMITER'])
    recordDelimiter = str(extendedProperty['UNLOAD_RECORD_CHAR'])
    fieldEnclosure = str(extendedProperty['UNLOAD_ENC'])
    if fieldEnclosure == "'":
        fieldEnclosure = "''"
    header = "2"


    bulk_load_sql = f"""
    BULK INSERT load_product_line_TMP
    FROM '{filePath}'
    WITH (
        FORMAT = 'CSV',
        FIELDTERMINATOR = '{fieldDelimiter}',
        ROWTERMINATOR = '{recordDelimiter}',
        FIELDQUOTE = '{fieldEnclosure}',
        FIRSTROW = {header},
        KEEPNULLS,
        CODEPAGE = '65001'    )
    """
    write_detail("=================== BULK LOAD ===================")
    write_detail(f"Running bulk load query for file: '{filePath}'")
    write_detail(bulk_load_sql)
    write_detail("=================================================")

    try:
        row_count = execute_sql_query(bulk_load_sql)
        return row_count, True
    except Exception as exceptionError:
        write_error("Failed to load data: " + str(exceptionError))
        write_error(traceback.format_exc())
        exitCode = 2
        exitMessage = 'Script failed to load data'
        raise exceptionError


# Insert Data Into Target Table
def insert_data():
    insert_sql = """
    INSERT INTO Stage.load_product_line                  (     product_line_code
                      ,     product_line_description
                      ,     creating_employee_id
                      ,     created_datetime
                      ,     last_change_employee_id
                      ,     last_change_datetime
                      )
    SELECT 
        product_line_code
            ,        product_line_description
            ,        creating_employee_id
            ,        created_datetime
            ,        last_change_employee_id
            ,        last_change_datetime
            FROM load_product_line_TMP
    """

    write_detail("=================== INSERT DATA ===================")
    write_detail("Running insert query")
    write_detail(insert_sql)
    write_detail("==================================================")

    try:
        row_count = execute_sql_query(insert_sql)
        return row_count
    except Exception as exceptionError:
        write_error("Failed to insert data: " + str(exceptionError))
        write_error(traceback.format_exc())
        exitCode = 2
        exitMessage = 'Script failed to insert data'
        raise exceptionError


# ========================================================
# = Main Process Start Here
# ========================================================
try:
        

    # -- Init Azure Blob Storage Utility --------------------------------------------
    connection_string = "DefaultEndpointsProtocol=https;AccountName={};AccountKey={};EndpointSuffix={}".format(extendedProperty['BLOB_STORAGE_ACCOUNT'], extendedProperty['BLOB_STORAGE_ACCESS_KEY'], extendedProperty['BLOB_ENDPOINT'])

    azure_blob_storage = WslPythonSQLServer.AzureBlobStorage(
        write_audit=write_audit,
        write_detail=write_detail,
        write_error=write_error,
        connection_string=connection_string,
        container_name=extendedProperty['BLOB_STORAGE_CONTAINER']
        )


    #--============================================================================
    #-- Start Load Process - Database
    #--============================================================================

    loadTableProperties['loadDetailList'] = []
    loadTableProperties['totalRowCount'] = 0

    write_audit("=================== Start Load Process - Database ===================")

    #--============================================================================
    #-- Create temporary files for Source Database
    #--============================================================================

    folderDownloadPath = os.path.join(loadTableProperties['WSL_WORKDIR'], "wsl" + '_' +str(os.environ.get('WSL_LOAD_TABLE','')) + '_' + str(os.environ.get('WSL_SEQUENCE','')))
    os.mkdir(folderDownloadPath)

    downloadedFileName = "wsl" + '_' + str(os.environ.get('WSL_LOAD_TABLE','')) + '_' + str(os.environ.get('WSL_SEQUENCE',''))

    write_audit("================= EXTRACT SQL =====================")

    extractSql = """
    SELECT product_line.product_line_code  AS product_line_code
    , product_line.product_line_description  AS product_line_description
    , product_line.creating_employee_id  AS creating_employee_id
    , product_line.created_datetime  AS created_datetime
    , product_line.last_change_employee_id  AS last_change_employee_id
    , product_line.last_change_datetime  AS last_change_datetime
    FROM dbo.product_line product_line
    """

    source = f"{str(os.environ.get('WSL_SRC_DB',''))}.{str(os.environ.get('WSL_SRC_SCHEMA',''))}.product_line"

    write_detail("BEGIN create of data file from source system: "+str(datetime.now()))

    write_audit(extractSql)

    dataFile= os.path.join(folderDownloadPath,downloadedFileName) + '.' + 'csv'.lower()
    try:
        sourceDbType = "SQL Server"
        result = WslPythonCommon.GetDataToFileV3(extractSql,str(os.environ.get('WSL_SRC_DSN','')),str(os.environ.get('WSL_SRC_USER','')),str(os.environ.get('WSL_SRC_PWD','')),
            dataFile,str(extendedProperty['UNLOAD_DELIMITER']), extendedProperty['SPLIT_THRESHOLD'], True, unicode,
            str(extendedProperty['UNLOAD_ENC']), str(extendedProperty['UNLOAD_ESC']), True, 'csv', None,sourceDbType)
    except Exception as exceptionError:
        write_error("Failed to generated datafiles: " + str(exceptionError))
        write_error(traceback.format_exc())
        exitCode = 2
        exitMessage = 'Script failed to create data file from source system'
        raise Exception('Script failed to create data file from source system')

    if result[0]== -2:
        error_message = result[1]
        write_error("Failed to generated datafiles: " + str(error_message))
        exitCode = 2
        exitMessage = 'Script failed to create data file from source system'
        raise Exception('Script failed to create data file from source system')
    elif result[0]==1 and result[1] < 1:
        write_audit("Source query returned 0 rows")

    write_detail("END create of data file from source system: "+str(datetime.now()))

    #For multiple input files add file name to list
    dataFiles = []
    for f in glob.iglob(os.path.join(folderDownloadPath,downloadedFileName)+'*.' + 'csv'): 
        dataFiles.append(f)

    #Set values in load table properties
    loadTableProperties['dataFileList']   =  dataFiles
    loadTableProperties['rowCount']       = result[1]
    loadTableProperties['sourceFilePath'] = folderDownloadPath

    # Load Data
    write_audit("================= LOAD DATA =====================")


    for dataFile in dataFiles:
        drop_temp_table()
        create_temp_table()
        load_start_time = datetime.now()
        row_count, status = load_data(dataFile)
        load_end_time = datetime.now()
        time_taken = load_end_time - load_start_time
        loadTableProperties['loadDetailList'].append(
            {'source' : f'product_line',
            'row_count' : row_count,
            'time_taken' : round(time_taken.total_seconds(), 2),
            'status' : 'Success' if status else 'Failed'}
            )
        insert_row_count = insert_data()
        loadTableProperties['totalRowCount'] += insert_row_count


    

    # Drop Temporary Table
    write_audit("================= DROP TEMP TABLE =====================")
    drop_temp_table()

    # Construct Load Summary Message
    write_audit("Source                                            " + "Status         " + "Rows Loaded        "+"Time Taken")
    write_audit("--------------------------------------------------  " + "-------------- " + "------------------ "+"----------")

    for loadDetail in loadTableProperties['loadDetailList']:
        write_audit(loadDetail['source'] + "  " + loadDetail['status'] + "  " + str(loadDetail['row_count']) + "  " + str(loadDetail['time_taken']) + " seconds")

    write_audit("")
    write_audit("Total Rows Inserted In Table: " + str(loadTableProperties['totalRowCount']))
    write_audit("Load Completed Successfully")

    if extendedProperty['DEBUG'].upper() == 'FALSE':

        shutil.rmtree(folderDownloadPath)
except Exception as e:
    write_error("Script Failed:" + str(e))
    write_error(traceback.format_exc())
    exitCode = 2
    exitMessage = 'Script run failed'
else:
    exitCode = 0
    exitMessage = 'Script ran successfully'
finally:
    exit_script(exitCode, exitMessage)
